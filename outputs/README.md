# Generated outputs

Every new run must use a dedicated `outputs/<run-id>/` directory. `archive/` contains
legacy CAD/USD results moved out of the repository root during layout cleanup.

Everything in this directory except this README is generated or locally archived and
is excluded from source control.
