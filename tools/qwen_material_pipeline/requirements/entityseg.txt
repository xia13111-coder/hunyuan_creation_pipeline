# Extra Python packages installed automatically by `qwen-material setup-models`.
#
# MMCV 1.x still calls yapf.FormatCode(..., verify=True).  YAPF 0.40.2 and
# newer removed that argument, so this compatibility pin is intentional.
yapf==0.40.1
cloudpickle>=2,<4
black==22.3.0
platformdirs>=3.5.1,<5
tomli>=2,<3
Pillow>=9,<10
mmcv==1.7.2
iopath>=0.1.7,<0.1.10
